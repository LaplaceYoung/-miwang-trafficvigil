import React from "react";
import ReactDOM from "react-dom/client";
import TrafficVigilPrototypeApp from "./TrafficVigilPrototypeApp.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TrafficVigilPrototypeApp />
  </React.StrictMode>
);
